/**
 * ============================================================
 * FIREBASE — IMPLEMENTACIÓN FUTURA (NO ACTIVA)
 * ============================================================
 *
 * ¿Qué hace?
 *   Alternativa a Supabase. Si prefieres Firebase (Firestore),
 *   este archivo se completa con una implementación de
 *   DataProvider usando firebase-admin/firebase-web SDK.
 *
 * ¿Cómo funciona (cuándo se haga)?
 *   1. npm install firebase
 *   2. Completar este archivo siguiendo el contrato ./provider.ts
 *   3. Poner DATABASE_PROVIDER=firebase en .env con las credenciales
 *   4. Cambiar el throw en ./index.ts por este proveedor
 *
 * ¿Dónde modificarlo?
 *   Archivo nuevo cuando se decida migrar a la nube.
 *
 * ¿Qué archivos utiliza?
 *   - ../provider.ts (contrato a implementar)
 *   - ../index.ts (donde se activa)
 */
export {};
